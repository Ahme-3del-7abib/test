<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.onetoonechatapp">

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.BIND_QUICK_ACCESS_WALLET_SERVICE" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OneToOneChatApp">

        <activity android:name=".ui.activities.chat.ChatActivity" />
        <activity android:name=".ui.activities.users.UserListActivity" />
        <activity android:name=".ui.activities.register.RegisterActivity" />
        <activity android:name=".ui.activities.login.LoginActivity" />
        <activity android:name=".ui.activities.user_listing.UserListingActivity" />

        <activity android:name=".ui.activities.splash.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>